services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - SESSION_SECRET=dev-secret
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
